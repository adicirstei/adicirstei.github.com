extends layout
block content
  :markdown
    ## ceva pe aici
    ### some javascript

    ```js
    var fn = function (x) {
      return (x + 3) * 5;
    }
    ```

    ### some python

    ```py
    x = [x * 2 for x in [1..4]]
    ```
  